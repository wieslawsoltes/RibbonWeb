name: Blazor
on:
  pull_request:
  push:
    branches: [main]
    paths: ['blazor/**', 'dotnet/**', 'src/**', 'package*.json', '.gitmodules', '.github/workflows/dotnet.yml']
  workflow_dispatch:
    inputs:
      publish:
        description: Publish the checked-in Blazor version from main
        type: boolean
        default: false
permissions:
  contents: read
jobs:
  blazor:
    permissions:
      contents: write
    uses: wieslawsoltes/Dockyard/.github/workflows/blazor.yml@9a894d7c647a180669853660fd2acec24eb2885a
    with:
      project: dotnet/RibbonWeb.Blazor.csproj
      package_name: RibbonWeb
      publish: ${{ github.event_name == 'workflow_dispatch' && inputs.publish }}
    secrets: inherit
